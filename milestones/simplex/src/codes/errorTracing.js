/**
 * Enum for common error codes.
 * @readonly
 * @enum {code}
 */
const ErrorCodes = Object.freeze({
    FRONTEND_PARSING_ERROR: 39,
    BACKEND_PARSING_ERROR: 37
})

module.exports = {ErrorCodes}
